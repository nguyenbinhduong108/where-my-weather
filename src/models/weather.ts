export type WeatherInfo = {
  region_name: string;
  dates: string[];
  temperatures: number[];
  precipitation: number[];
  humidity: number[];
  temperature_max: number[];
  temperature_min: number[];
};